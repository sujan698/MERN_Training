/*
"sujan"
'sujan'
`sujan` 

string can be defined by using single quote, double quote or backtick

limitation
     we can't use single quote inside single quote
     we can't use double quote inside double quote
     we can't use backtick inside backtick
 */